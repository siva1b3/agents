# Docker Compose reads .env if you choose to copy this file to .env.
# The default Compose configuration already enables the public demo accounts.
SEED_DEMO_DATA=true
SESSION_LIFETIME_SECONDS=3600
REQUEST_LIMIT_PER_MINUTE=120
AUTHENTICATION_LIMIT_PER_MINUTE=10
ALLOWED_ORIGINS=http://localhost:3000
